---
title: Introduction
description: The pebbles monorepo and package layout.
---

`pebbles` is split into a CLI, a reusable core package, and a private default registry.

Use the navigation to explore:

- **CLI**: how to run `pebbles`, override registries, and use the default bundle
- **Registry source**: how to validate registry documents with `@pebbles/core`
