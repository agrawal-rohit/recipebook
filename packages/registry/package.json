{
	"name": "@pebbles/registry",
	"version": "0.2.1",
	"description": "An opinionated pebbles registry with project tooling configurations and GitHub Actions workflows.",
	"license": "MIT",
	"private": true,
	"scripts": {
		"build": "pnpm --filter @pebbles/core run build && tsx ./scripts/build-registry.ts .",
		"typecheck": "pnpm --filter @pebbles/core run build && tsc -p tsconfig.json --noEmit",
		"test": "vitest run --passWithNoTests --exclude 'registry/**'"
	},
	"dependencies": {
		"@pebbles/core": "workspace:*",
		"spdx-license-list": "^6.12.0"
	}
}
